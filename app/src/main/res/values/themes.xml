<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- سِمة التطبيق الأساسيّة (الواجهة نفسها من Compose؛ هذه للنافذة) -->
    <style name="Theme.GtSalat" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <!-- شاشة البدء (core-splashscreen) -->
    <style name="Theme.GtSalat.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash</item>
        <item name="postSplashScreenTheme">@style/Theme.GtSalat</item>
    </style>
</resources>
